apiVersion: v1
kind: Service
metadata:
  name: {{ include "take2-product.fullname" . }}
  labels:
    {{- include "take2-product.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "take2-product.selectorLabels" . | nindent 4 }}
